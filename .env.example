PORT=8000

# ── Upscale driver ──────────────────────────────────────────────────────
# cpu     — Pillow LANCZOS (работает везде, качество ниже)
# intel   — Intel GPU через Vulkan (N100, требует --device /dev/dri)
# lavapipe — Lavapipe software Vulkan (Linux, медленно)
UPSCALE_DRIVER=cpu
